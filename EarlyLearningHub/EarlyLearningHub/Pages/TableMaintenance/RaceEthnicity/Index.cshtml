@page
@model EarlyLearningHub.Pages.TableMaintenance.RaceEthnicity.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RaceEthnicity[0].ReName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RaceEthnicity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ReId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ReId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ReId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
