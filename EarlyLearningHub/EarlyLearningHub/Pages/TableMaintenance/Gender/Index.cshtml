@page
@model EarlyLearningHub.Pages.TableMaintenance.Gender.IndexModel

@{
    ViewData["Title"] = "Gender";
}

@*<h2>Index</h2>*@

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gender[0].GeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gender[0].GeAbbreviation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Gender)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GeAbbreviation)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.GeId">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.GeId">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.GeId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
