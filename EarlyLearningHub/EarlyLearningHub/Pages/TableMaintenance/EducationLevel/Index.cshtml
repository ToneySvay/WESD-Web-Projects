@page
@model EarlyLearningHub.Pages.TableMaintenance.EducationLevel.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EducationLevel[0].ElName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EducationLevel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ElName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ElId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ElId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ElId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
