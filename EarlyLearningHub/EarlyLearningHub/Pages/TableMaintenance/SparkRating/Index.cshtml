@page
@model EarlyLearningHub.Pages.TableMaintenance.SparkRating.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SparkRating[0].SrName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SparkRating) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SrName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SrId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SrId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SrId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
