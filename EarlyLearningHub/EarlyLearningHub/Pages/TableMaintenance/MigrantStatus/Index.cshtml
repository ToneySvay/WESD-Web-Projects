@page
@model EarlyLearningHub.Pages.TableMaintenance.MigrantStatus.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MigrantStatus[0].MsOptions)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MigrantStatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MsOptions)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MsId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MsId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
